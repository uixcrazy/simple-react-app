import React from 'react';
import PropTypes from 'prop-types';
import Button from "Common/Button";

const App = props => {
  return (
    <div>
      simple react app
      <div>
        <Button />
      </div>
    </div>
  );
};

App.propTypes = {

};

export default App;